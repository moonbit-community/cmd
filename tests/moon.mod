name = "mooxCLI/cmd-tests"

version = "0.1.0"

readme = "README.md"

repository = "https://github.com/moonbit-community/cmd"

license = "Apache-2.0"

keywords = [ "command-line", "testing", "moonbit" ]

preferred_target = "native"

description = "Unified compatibility, policy, and pinned-oracle runner for the cmd workspace."

import {
  "cli/core@0.1.4",
  "moonbitlang/async@0.21.2",
  "moonbitlang/x@0.5.1",
}
