name = "mooxCLI/cmd"

version = "0.1.4"

readme = "README.md"

repository = "https://github.com/moonbit-community/cmd"

license = "Apache-2.0"

keywords = [ "command-line", "deprecated", "moonbit" ]

preferred_target = "wasm"

description = "Deprecated legacy command set. Use cli/<command> instead."
