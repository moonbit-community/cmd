# echo

Write arguments separated by one space. Supports `-n`, `-e`, and `-E`,
including the common backslash escapes and byte-valued octal/hex escapes.

```sh
moonx mooxCLI/cmd/echo -- -e 'hello\nworld'
```
