name = "cli/basename"

version = "0.2.0"

readme = "README.md"

repository = "https://github.com/moonbit-community/cmd"

license = "Apache-2.0"

keywords = [ "command-line", "moonx", "wasm" ]

preferred_target = "wasm"

description = "basename command for MoonX."

import {
  "cli/core@0.2.0",
  "moonbitlang/async@0.22.1",
  "moonbitlang/x@0.5.5",
}
