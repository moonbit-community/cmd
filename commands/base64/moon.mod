name = "cli/base64"

version = "0.1.0"

readme = "README.md"

repository = "https://github.com/moonbit-community/cmd"

license = "Apache-2.0"

keywords = [ "command-line", "moonx", "wasm" ]

preferred_target = "wasm"

description = "base64 command for MoonX."

import {
  "cli/core@0.1.0",
  "moonbitlang/async@0.21.0",
  "moonbitlang/x@0.5.1",
}
