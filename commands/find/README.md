# find

Walk directory trees without following symbolic links. Supports `-name` and
`-path` shell patterns, `-type`, depth limits, negation, AND/OR composition,
and newline or NUL output. Mutating and process-launching actions such as
`-delete` and `-exec` are intentionally outside this read-only command.
